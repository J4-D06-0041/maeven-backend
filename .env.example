# Example environment variables for maeven-backend
# Copy this to `.env` and fill in real values (do NOT commit secrets).

# Option A: Full connection string (preferred for hosted DBs like Render)
# DATABASE_URL=postgresql://username:password@host:5432/dbname?sslmode=require

# Option B: Individual connection values
# DB_HOST=your-db-host.example.com
# DB_PORT=5432
# DB_NAME=maeven_collections
# DB_USER=username
# DB_PASS=supersecret

# SSL settings (set to true for hosted providers that require TLS)
# DB_SSL=true
# DB_SSL_REJECT_UNAUTHORIZED=false

# App port
# PORT=3000
DB_HOST=your-db-host
DB_PORT=5432
DB_NAME=maeven_collections
DB_USER=core_dev
DB_PASS=change_me

PORT=3000
